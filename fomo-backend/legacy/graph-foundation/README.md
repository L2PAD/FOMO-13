# Retired graph-foundation cleanup

This destructive cleanup utility is retained only as historical reference. Its
collection list overlaps the active FOMO v2 graph, funding, and candidate
domains, so it is not exposed through `package.json` or compiled into the
production backend.

Do not run it against an active database.
