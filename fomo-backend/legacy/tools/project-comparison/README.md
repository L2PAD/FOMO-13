# Retired project comparison index migration

This script is retained only as historical reference. It creates indexes on
retired project intel, unlock, funding-round, and comparison collections and is
not exposed through `package.json` or compiled into the production backend.

Do not run it against an active database.
