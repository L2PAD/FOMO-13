# Legacy funding-round tools

These scripts target the retired `fundingrounds` collection. They are archived
for historical reference only and are not supported production commands.

The active funding data model and operational tooling live under
`src/fomo-v2/domains/funding`. The retired collection is empty, so no data
migration or backfill is required.
