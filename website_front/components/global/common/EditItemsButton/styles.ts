import styled from "styled-components";

export const EditButton = styled.button`
  display: flex;
  align-items: center;
  gap: 3px;
  color: var(--color-info);
  margin-top: 8px;
  svg {
    fill: var(--color-info);
  }
`;
